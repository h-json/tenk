<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- gal: 영상 합본 갤러리 저장용. API 30+ 는 MediaStore 라 권한 불필요, API 29 이하만 필요.
         camera_android_camerax 가 같은 permission 을 maxSdkVersion=28 로 선언해서 manifest merger 가 충돌 → tools:replace 로 덮어씀. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:replace="android:maxSdkVersion"/>
    <!-- 로컬 알림(기록 리마인더·종료 임박·확정 대기). POST_NOTIFICATIONS 는 flutter_local_notifications
         가 자기 매니페스트에도 선언하지만, Play 콘솔 권한 목록과 대조하기 쉽게 여기에도 명시한다.
         RECEIVE_BOOT_COMPLETED 가 없으면 재부팅 시 예약이 전부 날아가 앱을 다시 열 때까지 알림이 죽는다.
         ⚠️ SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM 은 일부러 선언하지 않는다 — 정확한 알람은
         알람시계·캘린더 앱 전용이라 TenK 은 자격이 없고, 분 단위가 중요한 알림도 아니다
         (CLAUDE.md "알림", decisions.md "알림 기능" 결정 5). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <queries>
        <package android:name="com.kakao.talk" />
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: 이용약관·개인정보처리방침을 외부 브라우저로 열기 위한 https VIEW 조회. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- url_launcher: 법적 고지의 '문의' 가 메일 앱을 열기 위한 mailto 조회.
             Android 11+ 패키지 가시성 제한 때문에 이 선언이 없으면 조용히 실패한다. -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:label="TenK"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.kakao.sdk.flutter.auth.AuthCodeHandlerActivity"
            tools:node="merge">
            <intent-filter android:label="kakao_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kakao${kakaoNativeAppKey}" android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- flutter_local_notifications: 예약 알림을 실제로 띄우는 리시버 + 재부팅/앱 업데이트 후
             예약을 복원하는 리시버. 둘 다 없으면 예약은 성공하는데 알림이 안 뜬다. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Impeller 비활성화 (Skia 폴백). 삼성 실기기에서 video_player 외부 텍스처가
             초당 10여 회 깜빡이는 Impeller 렌더링 버그 회피. 디코더/컨트롤러는 정상이고
             텍스처 합성 단계만 깜빡였음 (logcat: 단일 디코더 정상, 화면 전체 아닌 영상만).
             flutter run 의 no-enable-impeller 플래그로 원인 확정. 같은 프로젝트에서 Impeller
             텍스트 깨짐 이슈도 기록됨. Impeller 외부 텍스처 버그가 업스트림에서 고쳐지면 제거 검토. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
</manifest>
